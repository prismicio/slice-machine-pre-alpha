Recap:
- We created a new Nuxt project
- We created a new Prismic repository, accessible here: {{{ documentsUrl }}}
- We added a prismic.config.js file for you.
- We added slices and a SliceZone in ‘sliceMachine’ - a folder at the root of your project.
- We added examples in your ‘pages’

Additional `nuxt.config.js` configuration (roughly):

modules: [
  ['@nuxtjs/prismic', {
    endpoint: 'https://<REPOSITORY>.cdn.prismic.io/api/v2',
    linkResolver(doc) {
      if (doc.type === 'page') {
        return doc.uid === 'homepage' ? '/' : `/${doc.uid}`
      }
      return '/'
    },
    routes: [
      '/:uid',
      '/',
    ]
  }]
]

👆 Make sure you add/understand this before running the project


- After it's done, launch the project, run: `cd montest; npm run dev;`
